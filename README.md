# 7D Loader — manifest

Catalogue public des mods pour **7D Loader**.

- URL stable (utilisée par le launcher) :
  `https://raw.githubusercontent.com/13RDPALEON/7D-Loader-manifest/main/manifest.json`
- Les archives RAR restent sur Google Drive ; seuls les `file_id` / versions sont ici.

Ne renomme pas `manifest.json` et ne change pas la branche `main`.
